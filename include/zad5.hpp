#include "zad1.hpp"

#include <algorithm>
#include <deque>
#include <vector>

// tutaj algorytm liczKolejneWarzywa
template<typename T>
bool liczKolejneWarzywa(T iter1, T iter2, unsigned n, const std::string& s)
{
    Warzywo warz;
    if (search_n(iter1, iter2, n, warz, s) == poczatek) ? true : false;
}
